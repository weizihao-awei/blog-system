<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ykw.blog_system.mapper.ArticleTagMapper">
    
    <resultMap id="BaseResultMap" type="com.ykw.blog_system.entity.ArticleTag">
        <id column="id" property="id"/>
        <result column="article_id" property="articleId"/>
        <result column="tag_id" property="tagId"/>
    </resultMap>
    
    <sql id="Base_Column_List">
        id, article_id, tag_id
    </sql>
    
    <select id="selectByArticleId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM article_tag WHERE article_id = #{articleId}
    </select>
    
    <select id="selectByTagId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM article_tag WHERE tag_id = #{tagId}
    </select>
    
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO article_tag (article_id, tag_id) VALUES (#{articleId}, #{tagId})
    </insert>
    
    <insert id="insertBatch">
        INSERT INTO article_tag (article_id, tag_id) VALUES
        <foreach collection="tagIds" item="tagId" separator=",">
            (#{articleId}, #{tagId})
        </foreach>
    </insert>
    
    <delete id="deleteByArticleId">
        DELETE FROM article_tag WHERE article_id = #{articleId}
    </delete>
    
    <delete id="deleteByTagId">
        DELETE FROM article_tag WHERE tag_id = #{tagId}
    </delete>
</mapper>
